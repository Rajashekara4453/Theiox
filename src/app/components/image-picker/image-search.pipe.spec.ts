import { ImageSearchPipe } from './image-search.pipe';

describe('ImageSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
