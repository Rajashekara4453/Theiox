import { SidebarSearchPipe } from './sidebar-search.pipe';

describe('SidebarSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new SidebarSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
