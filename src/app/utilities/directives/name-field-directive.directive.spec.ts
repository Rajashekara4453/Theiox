import { NameFieldDirectiveDirective } from './name-field-directive.directive';

describe('NameFieldDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NameFieldDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
