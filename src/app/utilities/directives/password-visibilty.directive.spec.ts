import { PasswordVisibiltyDirective } from './password-visibilty.directive';

describe('PasswordVisibiltyDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordVisibiltyDirective();
    expect(directive).toBeTruthy();
  });
});
